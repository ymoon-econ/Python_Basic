# "^a" will match all strings that start with "a"
# "a$" will match all strings that end with "a"

# We can account for this inconsistency with square brackets. We use square brackets in a regex to indicate that any character within them can fill the space.

